var config = {
    'host': 'http://your.lats.host/lats',

    charts: [
            {
                tick: "second",           // minimum tick marker - can be second, minute, hour, month, year
                name: "UniqueSensorName", // name of the sensor
                span: 10000               // size of visible area in milliseconds
            },
            {
                tick: "minute",
                name: "UniqueSensorName",
                span: 60 * 60 * 1000
            }
        ]
};